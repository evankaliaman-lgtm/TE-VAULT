<x-app-layout><x-page-header title="Notifications" description="Read-only delivery history." /><div class="mt-6">@forelse($notifications as $notification)<article class="border-b border-slate-200 bg-white p-4"><p class="font-medium">{{ $notification->notification_type->value }}</p><p class="text-sm text-slate-500">{{ $notification->scheduled_for }}</p></article>@empty<x-empty-state title="No notifications" description="Notification history will appear here." />@endforelse</div><div class="mt-4">{{ $notifications->links() }}</div></x-app-layout>
